#include <stdio.h>
void dispalyString(char str[]);
int main()
{
    char str[50];
    printf("Enter String");
    gets(str);
    dispalyString(str);
    return 1;

    void displayString(char str[]);
    {
        printf("String Output");
        puts(str);
    }
}